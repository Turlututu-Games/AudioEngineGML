# UI Sound

UI type of sound are designed to not run in loop, and to persist accross room. It mainly can be used for extra-diegetic sounds, like menu, etc

An UI sound can be configured to randomize the volume or the pitch, to allow some variety.

It also possible to configure an array of sound assets. In this scenario, a random sound from this array will be selected each time